{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -c \"cyan,magenta\" \"pnpm --silent run dev:frontend\" \"pnpm --silent run dev:backend\"",
    "dev:frontend": "cross-env PORT=3000 BASE_PATH=/ pnpm --silent --filter @workspace/panda-nomad run dev",
    "dev:backend": "cross-env PORT=3001 pnpm --silent --filter @workspace/api-server run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
